# PHP-Practice

## setup
add alias to .bashrc file
alias sail='[ -f sail ] && sh sail || sh vendor/bin/sail'

compose install

sail up 

npm i && npm run dev

## start application

sail up
npm run dev
